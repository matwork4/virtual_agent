class Question {

  constructor(id, idScenario, textEN, textFR, textJP, idNextQuestion, isOpenQuestion, isFirst, isEnd, nameVariable){
  	this.id = id;
  	this.idScenario = idScenario;
  	this.textEN = textEN;
  	this.textFR = textFR;
  	this.textJP = textJP;
  	this.idNextQuestion = idNextQuestion;
  	this.isOpenQuestion = isOpenQuestion;
  	this.isFirst = isFirst;
  	this.isEnd = isEnd;
  	this.nameVariable = nameVariable;
  }

}